<h1><%= @post.title %></h1>

<p><%= @post.body %></p>

<h3>Comments</h3>
<% @comments.each do |c| %>
<p><%= c.body %></p>
<% end %>

<strong>Add new comment below ..</strong>
<%= form_for [@post, @comment] do |f| %>
  <p><%= f.text_area :body %></p>
  <%= f.submit %>
<% end %>