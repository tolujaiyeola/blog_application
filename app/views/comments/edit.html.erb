<h1><%= @post.title %></h1>

<p><%= @post.body %></p>

<h3>Comments</h3>

<p><%= @comment.body %> 
      <%= link_to "edit", edit_post_comment_path(@post,@comment) %> 
      <%= link_to "delete", post_comment_path(@post,@comment), method: :delete, data: {confirm: "Delete post?"} %></p>

<strong>Edit Comment ..</strong>

<%#<%= form_for :comment, url: post_comment_path(@post,@comment), method: :patch do |f| %>

<%= form_for [@post, @comment] do |f| %>
  <p><%= f.text_area :body %></p>
  <%= f.submit %>
<% end %>